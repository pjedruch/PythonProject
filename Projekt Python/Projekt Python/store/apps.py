from django.apps import AppConfig


class StoreConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'store'
#Klasa StoreConfig jest konfiguracją aplikacji store w projekcie Django, 
#zawierającą ustawienia domyślne dla modeli bazy danych i nazwę aplikacji.